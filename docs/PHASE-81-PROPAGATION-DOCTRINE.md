# Phase 81 - Propagation Doctrine

Status: doctrine.

This doctrine supports the sovereign doctrine source:
[OMI Constitutional Geometry](OMI-CONSTITUTIONAL-GEOMETRY.md).

## Purpose

Phase 80 audits message-like surfaces for authority collisions.
Phase 81 states the positive doctrine that explains why those surfaces still
cohere.

Propagation in OMI is descriptive geometry over declared structure. It is not
a new authority layer and not a new runtime.

## Core Doctrine

```text
Gauge is the local field law.
Wave is propagation through the field.
Receipt is the coherence witness.
Projection is the observed spectrum.
```

That means OMI treats computation as propagation through declared segmented
structure:

```text
declaration
-> gauge / local field law
-> wave / propagation through segments
-> receipt / coherence witness
-> projection / observed spectrum
```

## The Admissible Boundary

The ASCII space character (`0x20`) is the admissible propagation boundary.
Whitespace is not blankness. It is the declared boundary that makes
segmentation observable.

```text
0x00..0x1F = latent structural substrate
0x20       = admissible propagation boundary
0x21..0x7E = observable printable projection field
```

From that boundary law:

- segmentation is topology
- parsing is projection
- reading is traversal

## Authority Split

The receipt split remains explicit and unchanged:

- `identity_receipt` = declaration authority
- `projection_receipt` = readout / reconciliation witness
- `package_receipt` = transport serialization witness
- `view_receipt` = presentation state

Propagation doctrine does not collapse those roles. It explains how the same
declared structure can propagate through them without losing identity.

## S-P-O-M Mapping

Phase 76 derives S-P-O-M from canonical declarations.
Phase 81 gives the propagation reading of that derived surface:

- `S` = source
- `P` = relation
- `O` = target
- `M` = phase condition

So a triplet is not only a relation record. It is also a propagation event
through declared structure with a modality/phase state.

## How Earlier Phases Cohere

This doctrine explains the shared language across the stack:

- gauge/kernel = propagation field law
- overlays = interference / reconciliation
- timelines = phase walks
- narrative = semantic resonance
- block images = persistent propagation surfaces
- charts and barcodes = compressed vs expanded spectra

That is why the later phases keep sounding wave-like without changing the
authority model. They are all describing the same declared structure at
different propagation resolutions.

## Locked Statement

> Gauge is the local field law.
> Wave is propagation through the field.
> Receipt is the coherence witness.
> Projection is the observed spectrum.
