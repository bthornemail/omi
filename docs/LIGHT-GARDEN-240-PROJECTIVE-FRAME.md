# Light Garden 240 Projective Frame

The Light Garden intuition is normalized into OMI as the public projective
frame law.

## Core Statement

`240` is the public projective frame.

It can be understood as:

- `60` structural positions across
- `4` carrier-aligned public orientations

This produces:

- `240` public projection states

Within the broader OMI timing stack:

- `256` = full byte manifold
- `16` = reserved operator/control bank
- `240` = public/projective frame
- `60` = local sweep
- `7` = Fano selector
- `8` = byte/source-target cadence
- `5040` = master reconciliation period

In the Phase 54 closure witness this becomes:

- `60` = local sexagesimal closure sweep
- `4` = carrier-aligned orientation phase
- `240` = public closure frame

## OMI Interpretation

The Light Garden is not a separate authority path.

It is a hardware/projective witness for the already-normalized timing stack
used across:

- carrier receipts
- event packets
- ESP32 witness envelopes
- polyform coordinate witnesses

## Locked Statement

The 240 Light Garden is the public projective frame:

`60 structural positions × 4 carrier-aligned orientations`

It belongs to the same OMI timing law as `16 / 7 / 8 / 5040`, and it remains a
projection witness rather than a replacement for canonical replay.
