# Phase 97 - 64-Ion Blackboard Pairing Doctrine

Phase 97 declares the missing pair-wise notation layer between the Phase 85
orientation incidence board and a future live blackboard resolver.

Locked rule:

> **The 64-ion surface is the pair-wise notation layer that lets trigintaduonion triads operate as blackboard resolver frames over OMI boundaries.**

## Distinction

The trigintaduonion layer supplies local incidence frames:

```text
31 imaginary units
155 triad incidence lines
each triad = local S-P-O frame
```

The blackboard requires a doubled surface because resolution is pair-wise:

```text
pending pattern <-> resolver frame
source <-> target
claim <-> evidence
question <-> answer
car <-> cdr
read <-> write
local <-> remote
```

Therefore:

```text
trigintaduonion = incidence triad board
64-ion surface  = pair-wise blackboard notation surface
```

## OMI Surface Compatibility

The 64-ion continuation is treated as compatible with the first 64 OMI
codepoints:

```text
0x00..0x1F = latent/control/incidence half
0x20..0x3F = operated/projectable/measurement half
0x00..0x3F = complete 64-state constitutional surface
0x40..0x7F = projected symbolic expansion / readable dual surface
```

This is compatibility doctrine, not algebraic authority. The 64-ion model helps
name pair-wise blackboard notation; canonical declarations and receipts remain
authority.

## Blackboard Pair

A blackboard does not merely store facts. In OMI, it pairs unresolved boundary
state with an admissible orientation frame:

```text
pending declaration
-> paired with orientation frame
-> resolved through triad
-> emitted as S-P-O-M
-> witnessed by receipt
```

The canonical declared pair shape is:

```lisp
(blackboard-pair
  (left pending-pattern)
  (right selected-frame)
  (orientation dual)
  (resolver receipt-seeded)
  (admission receipt-verified))
```

## Guardrails

```text
64-ion algebra is compatibility model, not authority.
Trigintaduonion triads are incidence frames, not truth.
Blackboard resolution is receipt-seeded.
Canonical declarations + receipts remain authority.
```

This phase adds no full algebra runtime, no neural backend, and no live
distributed blackboard.
